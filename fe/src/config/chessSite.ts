export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Vite + HeroUI",
  description: "Navbar with a few placeholder links.",
  navItems: [
    {
      label: "Home",
      href: "/",
    },
  ],
};